name: Generate Content (nightly)

on:
  schedule:
    - cron: "0 2 * * *"   # The nominal run targets the following Eastern morning.
  workflow_dispatch:
    inputs:
      date:
        description: "Eastern target date (YYYY-MM-DD); blank uses Eastern tomorrow"
        required: false
        type: string

permissions:
  actions: read
  contents: write
  pull-requests: write

jobs:
  generate:
    runs-on: ubuntu-latest
    # Resolve once from the original run metadata, not the delayed runner clock.
    env:
      TZ: America/New_York
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Resolve intended batch date once
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_EVENT: ${{ github.event_name }}
          RUN_SCHEDULE: ${{ github.event.schedule }}
          INPUT_DATE: ${{ inputs.date }}
        run: |
          RUN_CREATED_AT="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" --jq .created_at)"
          python scripts/generation_date.py --lane layer8culture \
            --event "$RUN_EVENT" --run-created-at "$RUN_CREATED_AT" \
            --cron "$RUN_SCHEDULE" --date "$INPUT_DATE" --github-env "$GITHUB_ENV"

      - name: Install Copilot CLI
        run: npm install -g @github/copilot

      - name: Generate posts with Copilot CLI
        id: copilot-generate
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.COPILOT_CLI_TOKEN }}
        # web-fetch + --allow-all-urls let the agent research the day's AI news.
        # Writes stay scoped to queue/* (the agent only reads the web). For a
        # stricter setup, replace --allow-all-urls with one or more
        # --allow-url=<domain> flags for a curated source allow-list.
        run: |
          copilot -p "$(cat scripts/generation-prompt.md)"$'\n\n'"$GENERATION_TARGET_INSTRUCTION" \
            -s \
            --allow-tool='read,write(queue/*),web-fetch' \
            --allow-all-urls \
            --no-ask-user

      - name: Verify, recover, and repair queue JSON
        env:
          GH_TOKEN: ${{ secrets.COPILOT_CLI_TOKEN }}
          FIRST_COPILOT_OUTCOME: ${{ steps.copilot-generate.outcome }}
        # The Copilot step above MUST write the resolved QUEUE_FILE. If the agent
        # instead saves the complete JSON under ~/.copilot/session-state/.../files,
        # recover the matching artifact before paying for a second generation. Retry
        # Copilot once only when recovery cannot find a valid queue artifact, then
        # attempt recovery one final time and fail with diagnostics if the queue is
        # still missing or invalid.
        run: |
          set -u

          QUEUE_DATE="$BATCH_DATE"

          run_copilot() {
            copilot -p "$(cat scripts/generation-prompt.md)"$'\n\n'"$GENERATION_TARGET_INSTRUCTION" \
              -s \
              --allow-tool='read,write(queue/*),web-fetch' \
              --allow-all-urls \
              --no-ask-user
          }

          recover_queue() {
            python scripts/recover_copilot_queue.py "$QUEUE_FILE" \
              --summary-file "$SUMMARY_FILE" \
              --date "$QUEUE_DATE"
          }

          if [ "$FIRST_COPILOT_OUTCOME" != "success" ]; then
            echo "::warning::Initial Copilot CLI step ended with outcome '$FIRST_COPILOT_OUTCOME'; checking disk and session artifacts before retrying."
          fi

          if [ ! -f "$QUEUE_FILE" ]; then
            echo "::warning::Copilot CLI did not write $QUEUE_FILE. Attempting recovery from Copilot session artifacts."
            if ! recover_queue; then
              echo "::warning::No valid Copilot artifact recovered for $QUEUE_FILE. Retrying Copilot generation once."
              set +e
              run_copilot
              RETRY_STATUS=$?
              set -e
              if [ "$RETRY_STATUS" -ne 0 ]; then
                echo "::warning::Copilot CLI retry exited with status $RETRY_STATUS; attempting final session-artifact recovery."
              fi
              if [ ! -f "$QUEUE_FILE" ]; then
                recover_queue || true
              fi
            fi
          fi

          if [ ! -f "$QUEUE_FILE" ]; then
            echo "::error::Copilot CLI did not write or recover $QUEUE_FILE after one retry."
            echo "queue/ currently contains:"
            ls -la queue/ || true
            echo "Matching Copilot session artifacts:"
            find "$HOME/.copilot/session-state" -path "*/files/${QUEUE_DATE}.json" -type f -printf '%TY-%Tm-%Td %TH:%TM:%TS %p\n' 2>/dev/null | sort -r | head -20 || true
            exit 1
          fi

          echo "Confirmed $QUEUE_FILE exists ($(wc -c < "$QUEUE_FILE") bytes)."
          python scripts/queue_json_guard.py "$QUEUE_FILE" --repair

      - name: Assign and validate posting schedule
        run: |
          python scripts/schedule_planner.py "$QUEUE_FILE" --date "$BATCH_DATE"
          python scripts/schedule_planner.py "$QUEUE_FILE" --for-publish --date "$BATCH_DATE"

      - name: Verify summary has no internal process warnings
        run: |
          if [ -f "$SUMMARY_FILE" ]; then
            if grep -Eiq 'permission issue|write access|session artifact|\\.copilot/session-state|copy that file|manual.*queue|tool name|allow-tool' "$SUMMARY_FILE"; then
              echo "::error::$SUMMARY_FILE contains internal process or permission-warning text. Re-run generation after fixing the summary."
              sed -n '1,80p' "$SUMMARY_FILE"
              exit 1
            fi
          fi

      - name: Install ffmpeg (for Reels)
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Generate visuals via OpenAI + Reels via ffmpeg
        env:
          # Image backend: Azure OpenAI is used when the AZURE_OPENAI_* secrets
          # are set (deployment-based); otherwise it falls back to direct OpenAI.
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_IMAGE_DEPLOYMENT: ${{ secrets.AZURE_OPENAI_IMAGE_DEPLOYMENT }}
          AZURE_OPENAI_API_VERSION: ${{ vars.AZURE_OPENAI_API_VERSION }}
          # Premium image defaults: quality "high" + 2K master (empty/unset = high/on).
          OPENAI_IMAGE_QUALITY: ${{ vars.OPENAI_IMAGE_QUALITY }}
          OPENAI_IMAGE_2K: ${{ vars.OPENAI_IMAGE_2K }}
          OPENAI_IMAGE_LONG_EDGE: ${{ vars.OPENAI_IMAGE_LONG_EDGE }}
          # Reel backend: Sora-2 (Azure) renders format:"reel" posts; it reuses the
          # AZURE_OPENAI_ENDPOINT/KEY above and falls back to ffmpeg motion if unset.
          AZURE_OPENAI_VIDEO_DEPLOYMENT: ${{ vars.AZURE_OPENAI_VIDEO_DEPLOYMENT }}
          AZURE_OPENAI_VIDEO_API_VERSION: ${{ vars.AZURE_OPENAI_VIDEO_API_VERSION }}
          AZURE_OPENAI_VIDEO_SIZE: ${{ vars.AZURE_OPENAI_VIDEO_SIZE }}
        run: |
          pip install openai requests pillow
          # Only process this run's exact generated or recovered queue.
          echo "Generating images for $QUEUE_FILE"
          python scripts/openai_gen.py "$QUEUE_FILE"
          # Animate any format:"reel" posts into mp4s (uses the stills above).
          echo "Rendering reels for $QUEUE_FILE"
          python scripts/reel_gen.py "$QUEUE_FILE"

      - name: Gate approval PR on schema, schedule, and final media
        run: |
          python scripts/queue_json_guard.py "$QUEUE_FILE"
          python scripts/schedule_planner.py "$QUEUE_FILE" --for-publish --date "$BATCH_DATE"
          python scripts/batch_readiness.py "$QUEUE_FILE"

      - name: Open approval PR
        # The org disables "Allow GitHub Actions to create pull requests", so the
        # built-in GITHUB_TOKEN cannot open the approval PR. We create it with a
        # user PAT (ENGINE_PR_TOKEN, repo scope) instead — a PR authored by a real
        # user is not subject to that restriction. The branch push still uses the
        # checkout-persisted GITHUB_TOKEN; only `gh pr create` reads GH_TOKEN.
        env:
          GH_TOKEN: ${{ secrets.ENGINE_PR_TOKEN }}
        run: |
          DATE="$BATCH_DATE"
          BRANCH="posts/$DATE"
          git config user.name "layer8-engine"
          git config user.email "engine@layer8culture.io"
          git checkout -b "$BRANCH"
          # -f on assets/generated so the freshly rendered media are included in
          # the approval PR for review (the dir is .gitignored for local hygiene).
          # Captures images, carousel slides, reel mp4s and their cover frames.
          git add queue/
          git add -f assets/generated
          git commit -m "Posts for $DATE — ready for review"
          git push origin "$BRANCH"
          # Build a visual PR body (image(s) + exact caption + hashtags) so the
          # reviewer sees what will post, Postiz-style, right where they approve.
          # Images embed by the just-pushed commit SHA via raw.githubusercontent.com
          # (immutable, survives branch deletion on merge; relies on a public repo).
          SHA=$(git rev-parse HEAD)
          python scripts/build_pr_preview.py "$QUEUE_FILE" \
                 --repo "${{ github.repository }}" \
                 --sha "$SHA" \
                 --out pr-body.md
          gh pr create --title "Posts for $DATE" \
                       --body-file pr-body.md \
                       --base main --head "$BRANCH"
